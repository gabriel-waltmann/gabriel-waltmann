import { Typography } from "@mui/material";

export default function Logo() {
  const sx = { my: 2, color: '#ffff' }
  return (
    <Typography variant="h6" sx={sx} id='logo'>
      &lt;Gabriel	&frasl;&gt;
    </Typography>
  )
}