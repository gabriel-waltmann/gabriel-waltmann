import { Typography } from "@mui/material";

export default function Logo(){
    return(
        <Typography variant="h6" sx={{ my: 2, color: '#ffff' }} id='logo'>
        &lt;Gabriel	&frasl;&gt;
      </Typography>
    )
}